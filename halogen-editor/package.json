{
  "private": true,
  "scripts": {
    "build": "pulp build --to dist/app.js",
    "minimize": "uglifyjs dist/app.js --output dist/min.js --compress",
    "watch": "pulp -w build --to dist/app.js"
  },
  "devDependencies": {
    "pulp": "^12.3.0",
    "purescript": "^0.12.0",
    "purescript-psa": "^0.5.1",
    "webpack": "^3.6.0"
  }
}
